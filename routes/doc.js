import { Router } from "express"
/**
 * Creates the different doc routes of the /doc/* endpoint
 * @param {Router} router Router of the /doc endpoint
 * @returns {Router} Router of the /doc endpoint
 */
export default function (router) {
    return router;
}